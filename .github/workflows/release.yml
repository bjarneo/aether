name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-linux:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            arch: amd64
          - os: ubuntu-24.04-arm
            arch: arm64
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev pkg-config

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.11.0

      - name: Build
        run: wails build -tags webkit2_41

      - name: Prepare release artifact
        run: |
          mv build/bin/aether build/bin/aether-linux-${{ matrix.arch }}
          chmod +x build/bin/aether-linux-${{ matrix.arch }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: aether-linux-${{ matrix.arch }}
          path: build/bin/aether-linux-${{ matrix.arch }}

  build-macos:
    strategy:
      matrix:
        include:
          - os: macos-latest
            arch: arm64
          - os: macos-13
            arch: amd64
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.11.0

      - name: Build
        run: wails build

      - name: Prepare release artifact
        run: |
          cd build/bin
          zip -r aether-macos-${{ matrix.arch }}.zip Aether.app

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: aether-macos-${{ matrix.arch }}
          path: build/bin/aether-macos-${{ matrix.arch }}.zip

  release:
    needs: [build-linux, build-macos]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Generate Changelog
        id: changelog
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ github.ref_name }}
          GITHUB_REPO: ${{ github.repository }}
        run: |
          PREVIOUS_TAG=$(git tag --sort=-version:refname | grep -v "^${TAG_NAME}$" | head -n 1)

          if [ -z "$PREVIOUS_TAG" ]; then
            PREVIOUS_TAG=$(git rev-list --max-parents=0 HEAD)
          fi

          CHANGELOG=""

          while IFS='|' read -r hash title author email; do
            short_hash="${hash:0:7}"
            username=$(gh api "repos/${GITHUB_REPO}/commits/$hash" --jq '.author.login' 2>/dev/null || echo "")

            if [ -n "$username" ]; then
              author_info="$author (@$username)"
            else
              author_info="$author"
            fi

            if [[ "$title" =~ ^Merge\ pull\ request\ \#([0-9]+) ]]; then
              pr_number="${BASH_REMATCH[1]}"
              pr_title=$(gh pr view "$pr_number" --json title --jq '.title' 2>/dev/null || echo "$title")
              CHANGELOG="${CHANGELOG}- $pr_title (#$pr_number) by $author_info ([\`$short_hash\`](https://github.com/${GITHUB_REPO}/commit/$hash))"$'\n'
            else
              CHANGELOG="${CHANGELOG}- $title by $author_info ([\`$short_hash\`](https://github.com/${GITHUB_REPO}/commit/$hash))"$'\n'
            fi
          done < <(git log "$PREVIOUS_TAG".."$TAG_NAME" --pretty=format:"%H|%s|%an|%ae")

          echo "$CHANGELOG" > changelog.md
          echo "changelog<<EOF" >> "$GITHUB_OUTPUT"
          echo "$CHANGELOG" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      - name: Calculate SHA256 of source archive
        id: archive
        env:
          TAG_NAME: ${{ github.ref_name }}
          GITHUB_REPO: ${{ github.repository }}
        run: |
          wget -q "https://github.com/${GITHUB_REPO}/archive/${TAG_NAME}.tar.gz"
          SHA256=$(sha256sum "${TAG_NAME}.tar.gz" | awk '{print $1}')
          echo "sha256=$SHA256" >> "$GITHUB_OUTPUT"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          generate_release_notes: false
          files: |
            artifacts/aether-linux-amd64/aether-linux-amd64
            artifacts/aether-linux-arm64/aether-linux-arm64
            artifacts/aether-macos-arm64/aether-macos-arm64.zip
            artifacts/aether-macos-amd64/aether-macos-amd64.zip
          body: |
            ## What's Changed

            ${{ steps.changelog.outputs.changelog }}

            ## Installation

            ### From AUR
            ```bash
            yay -S aether
            ```

            ### Pre-built Binary (x86_64)
            ```bash
            wget https://github.com/bjarneo/aether/releases/download/${{ github.ref_name }}/aether-linux-amd64
            chmod +x aether-linux-amd64
            sudo mv aether-linux-amd64 /usr/local/bin/aether
            ```

            ### Pre-built Binary (aarch64)
            ```bash
            wget https://github.com/bjarneo/aether/releases/download/${{ github.ref_name }}/aether-linux-arm64
            chmod +x aether-linux-arm64
            sudo mv aether-linux-arm64 /usr/local/bin/aether
            ```

            ### macOS (Apple Silicon)
            ```bash
            curl -LO https://github.com/bjarneo/aether/releases/download/${{ github.ref_name }}/aether-macos-arm64.zip
            unzip aether-macos-arm64.zip
            mv Aether.app /Applications/
            ```

            ### macOS (Intel)
            ```bash
            curl -LO https://github.com/bjarneo/aether/releases/download/${{ github.ref_name }}/aether-macos-amd64.zip
            unzip aether-macos-amd64.zip
            mv Aether.app /Applications/
            ```

            ### Build from Source
            Requires Go 1.23+, Node.js 22+, and [Wails](https://wails.io/docs/gettingstarted/installation).
            ```bash
            git clone https://github.com/bjarneo/aether.git
            cd aether
            wails build
            ```

            ---

            **SHA256 (source archive, for PKGBUILD):**
            ```
            ${{ steps.archive.outputs.sha256 }}
            ```
